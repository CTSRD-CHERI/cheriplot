from setuptools import setup

setup(name='cheri_mem_analysis',
      version='0.1',
      description='Trace tools for CHERI trace analysis',
      author='Alfredo Mazzinghi',
      packages=['cheri_trace_parser'])
